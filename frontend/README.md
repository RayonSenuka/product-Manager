# Product Catalog - Frontend

React + Vite frontend for the WMT Lab Test Practice.

## Setup

1. Install dependencies: `npm install`
2. Run: `npm run dev`
3. Open: http://localhost:5173

## Before Deployment

Update the `API_URL` in `src/api/productApi.js` to your deployed backend URL.
